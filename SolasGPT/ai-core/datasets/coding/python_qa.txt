[CODE] User: write a python function to add two numbers
Assistant: Here's a simple function: def add(a, b): return a + b. You can call it like result = add(5, 3).

[CODE] User: how to write a function to add numbers
Assistant: def add(a, b): return a + b. This takes two parameters and returns their sum.

[CODE] User: create a function that adds two numbers
Assistant: def add_numbers(x, y): return x + y. Example: print(add_numbers(10, 20)) outputs 30.

[CODE] User: write code to add two numbers
Assistant: def add(a, b): return a + b. Or simply: result = num1 + num2.

[CODE] User: python function for addition
Assistant: def add(a, b): return a + b. Usage: total = add(7, 8).

[CODE] User: how to write a for loop in python
Assistant: for i in range(5): print(i). This prints numbers 0 through 4.

[CODE] User: write a for loop example
Assistant: for item in items: print(item). Or: for i in range(10): do_something(i).

[CODE] User: how to write a while loop
Assistant: while condition: do_something(). Example: while x < 10: x += 1.

[CODE] User: write code to read a file
Assistant: with open('file.txt', 'r') as f: content = f.read(). This safely reads the entire file.

[CODE] User: how to read a file in python
Assistant: with open('filename.txt', 'r') as f: data = f.read(). The 'with' statement automatically closes the file.

[CODE] User: write a function to reverse a string
Assistant: def reverse_string(s): return s[::-1]. Example: reverse_string('hello') returns 'olleh'.

[CODE] User: how to reverse a string
Assistant: Use slicing: reversed_str = original[::-1]. Or: ''.join(reversed(original)).

[CODE] User: write code to sort a list
Assistant: sorted_list = sorted(my_list). Or in-place: my_list.sort().

[CODE] User: how to sort a list in python
Assistant: Use sorted(): new_list = sorted(old_list). Or sort in place: my_list.sort().

[CODE] User: write a function to find max in list
Assistant: def find_max(lst): return max(lst). Or manually: def find_max(lst): result = lst[0]; for num in lst: if num > result: result = num; return result.

[CODE] User: how to find maximum value in a list
Assistant: Use max(): largest = max(my_list). Or: largest = sorted(my_list)[-1].

[CODE] User: write code to check if number is even
Assistant: def is_even(n): return n % 2 == 0. Returns True for even numbers, False for odd.

[CODE] User: how to check if a number is even or odd
Assistant: Use modulo: if num % 2 == 0: print('even') else: print('odd').

[CODE] User: write a function to calculate factorial
Assistant: def factorial(n): return 1 if n <= 1 else n * factorial(n - 1). Or iterative: def factorial(n): result = 1; for i in range(2, n + 1): result *= i; return result.

[CODE] User: how to calculate factorial in python
Assistant: import math; result = math.factorial(5). Or manually with a loop: result = 1; for i in range(1, n + 1): result *= i.

[CODE] User: write code to remove duplicates from list
Assistant: unique_list = list(set(my_list)). Or preserve order: unique = list(dict.fromkeys(my_list)).

[CODE] User: how to remove duplicates from a list
Assistant: Convert to set: unique = list(set(my_list)). Note: this doesn't preserve order. For ordered: seen = set(); result = [x for x in my_list if not (x in seen or seen.add(x))].

[CODE] User: write a function to count vowels in string
Assistant: def count_vowels(s): return sum(1 for c in s.lower() if c in 'aeiou'). Example: count_vowels('hello') returns 2.

[CODE] User: how to count vowels in a string
Assistant: vowels = 'aeiou'; count = sum(1 for char in text.lower() if char in vowels).

[CODE] User: write code to merge two dictionaries
Assistant: merged = {**dict1, **dict2}. Or: dict1.update(dict2). In Python 3.9+: merged = dict1 | dict2.

[CODE] User: how to merge dictionaries in python
Assistant: Use unpacking: merged = {**d1, **d2}. Or update: d1.update(d2) modifies d1 in place.

[CODE] User: write a function to check palindrome
Assistant: def is_palindrome(s): clean = ''.join(c.lower() for c in s if c.isalnum()); return clean == clean[::-1].

[CODE] User: how to check if string is palindrome
Assistant: return text == text[::-1]. For case-insensitive: return text.lower() == text.lower()[::-1].

[CODE] User: write code to generate random number
Assistant: import random; num = random.randint(1, 100). This generates a random integer between 1 and 100.

[CODE] User: how to generate random numbers in python
Assistant: import random; random.randint(a, b) for integers. random.random() for float between 0 and 1. random.choice(list) for random element.

[CODE] User: write a function to flatten nested list
Assistant: def flatten(lst): return [item for sublist in lst for item in sublist]. For deeply nested: from itertools import chain; list(chain.from_iterable(nested)).

[CODE] User: how to flatten a nested list
Assistant: Use list comprehension: flat = [item for sublist in nested for item in sublist]. Or: from itertools import chain; flat = list(chain(*nested)).

[CODE] User: write code to convert string to list
Assistant: char_list = list(my_string). Or split by delimiter: word_list = my_string.split(' ').

[CODE] User: how to convert string to list in python
Assistant: Use list(): chars = list('hello'). Or split(): words = 'hello world'.split().

[CODE] User: write a function to find common elements
Assistant: def common_elements(list1, list2): return list(set(list1) & set(list2)). Returns elements present in both lists.

[CODE] User: how to find common elements in two lists
Assistant: Use set intersection: common = list(set(list1) & set(list2)). Or: common = [x for x in list1 if x in list2].

[CODE] User: write code to swap two variables
Assistant: a, b = b, a. Python's tuple unpacking makes swapping elegant and efficient.

[CODE] User: how to swap two variables in python
Assistant: Use tuple unpacking: x, y = y, x. No temporary variable needed.

[CODE] User: write a function to capitalize first letter
Assistant: def capitalize(s): return s.capitalize(). Or: return s[0].upper() + s[1:] if s else s.

[CODE] User: how to capitalize first letter of string
Assistant: Use capitalize(): result = text.capitalize(). Or title() for each word: text.title().

[CODE] User: write code to remove whitespace from string
Assistant: cleaned = my_string.strip(). For all whitespace: cleaned = ''.join(my_string.split()).

[CODE] User: how to remove whitespace in python
Assistant: Use strip() for edges: text.strip(). For all: text.replace(' ', ''). Or: ''.join(text.split()).

[CODE] User: write a function to check if list is empty
Assistant: def is_empty(lst): return len(lst) == 0. Or simply: return not lst.

[CODE] User: how to check if list is empty
Assistant: Use truthiness: if not my_list: print('empty'). Or: if len(my_list) == 0.

[CODE] User: write code to get list length
Assistant: length = len(my_list). Works for lists, strings, tuples, and other sequences.

[CODE] User: how to get length of list in python
Assistant: Use len(): size = len(my_list). Also works for strings, tuples, sets, and dictionaries.

[CODE] User: write a function to multiply all elements in list
Assistant: def multiply_all(lst): result = 1; for num in lst: result *= num; return result. Or: from functools import reduce; reduce(lambda x, y: x * y, lst).

[CODE] User: how to multiply all elements in a list
Assistant: from functools import reduce; import operator; result = reduce(operator.mul, my_list, 1).

[CODE] User: write code to convert list to string
Assistant: result = ''.join(my_list). For strings with spaces: result = ' '.join(my_list).

[CODE] User: how to convert list to string
Assistant: Use join(): text = ''.join(char_list). Or with separator: text = ', '.join(word_list).

[CODE] User: write a function to find sum of list
Assistant: def sum_list(lst): return sum(lst). Built-in sum() is most efficient.

[CODE] User: how to sum all elements in a list
Assistant: Use sum(): total = sum(my_list). Or manually: total = 0; for num in my_list: total += num.

[CODE] User: write code to create dictionary from lists
Assistant: keys = ['a', 'b', 'c']; values = [1, 2, 3]; my_dict = dict(zip(keys, values)).

[CODE] User: how to create dictionary from two lists
Assistant: Use zip(): my_dict = dict(zip(keys_list, values_list)). Example: dict(zip(['a', 'b'], [1, 2])) gives {'a': 1, 'b': 2}.

[CODE] User: write a function to filter even numbers
Assistant: def filter_evens(lst): return [x for x in lst if x % 2 == 0]. Or: list(filter(lambda x: x % 2 == 0, lst)).

[CODE] User: how to filter list in python
Assistant: Use list comprehension: evens = [x for x in numbers if x % 2 == 0]. Or filter(): evens = list(filter(lambda x: x % 2 == 0, numbers)).

[CODE] User: write code to handle exceptions
Assistant: try: risky_operation() except Exception as e: print(f'Error: {e}'). Add finally: for cleanup code.

[CODE] User: how to use try except in python
Assistant: try: code_that_might_fail() except ValueError: handle_error() except Exception as e: print(e) finally: cleanup().

[CODE] User: write a function to get current date
Assistant: from datetime import datetime; def get_date(): return datetime.now(). Or: datetime.now().strftime('%Y-%m-%d').

[CODE] User: how to get current date in python
Assistant: from datetime import datetime; today = datetime.now(). Format: today.strftime('%Y-%m-%d').

[CODE] User: write code to sleep for seconds
Assistant: import time; time.sleep(5). Pauses execution for 5 seconds.

[CODE] User: how to add delay in python
Assistant: import time; time.sleep(seconds). Example: time.sleep(2) pauses for 2 seconds.

[CODE] User: write a function to check if key exists in dict
Assistant: def key_exists(d, key): return key in d. Returns True if key is present.

[CODE] User: how to check if key exists in dictionary
Assistant: Use 'in': if 'key' in my_dict: do_something(). Or: my_dict.get('key') returns None if not found.

[CODE] User: write code to import module
Assistant: import module_name. Or specific: from module import function. Alias: import numpy as np.

[CODE] User: how to import library in python
Assistant: import library_name. For specific functions: from library import function. With alias: import pandas as pd.

[CODE] User: write a function to print formatted string
Assistant: def print_formatted(name, age): print(f'Name: {name}, Age: {age}'). F-strings are the modern way.

[CODE] User: how to format strings in python
Assistant: Use f-strings: f'Hello {name}'. Or format(): 'Hello {}'.format(name). Or %: 'Hello %s' % name.

[CODE] User: what is a syntax error
Assistant: A syntax error occurs when Python can't parse your code. Common causes: missing colons, unmatched parentheses, incorrect indentation. Fix: check error line carefully.

[CODE] User: how to debug python code
Assistant: Use print() statements to trace values. Or: import pdb; pdb.set_trace() for interactive debugging. IDE debuggers are also helpful.

[CODE] User: what is a variable in python
Assistant: A variable stores data. Create with assignment: x = 5. No type declaration needed. Variables can be reassigned to different types.

[CODE] User: how to define a variable
Assistant: Simply assign: variable_name = value. Examples: x = 10, name = 'Alice', items = [1, 2, 3].

[CODE] User: what is a list in python
Assistant: A list is an ordered, mutable collection. Create with brackets: my_list = [1, 2, 3]. Access with index: my_list[0]. Supports append, remove, etc.

[CODE] User: what is a dictionary
Assistant: A dictionary stores key-value pairs. Create with braces: d = {'name': 'Alice', 'age': 30}. Access: d['name'].

[CODE] User: how to create an empty list
Assistant: empty_list = []. Or: empty_list = list().

[CODE] User: how to create an empty dictionary
Assistant: empty_dict = {}. Or: empty_dict = dict().

[CODE] User: what is the difference between list and tuple
Assistant: Lists are mutable (can change): [1, 2, 3]. Tuples are immutable (cannot change): (1, 2, 3). Tuples are faster and hashable.

[CODE] User: how to use if statement
Assistant: if condition: do_something(). With else: if x > 5: print('big') else: print('small'). Add elif for multiple conditions.

[CODE] User: write an if else example
Assistant: if age >= 18: print('adult') elif age >= 13: print('teen') else: print('child').

[CODE] User: what is lambda function
Assistant: Anonymous function: lambda x: x * 2. Example: square = lambda x: x ** 2. Used for short, simple operations.

[CODE] User: how to use lambda in python
Assistant: lambda arguments: expression. Example: add = lambda a, b: a + b. Often used with map/filter: list(map(lambda x: x * 2, numbers)).

[CODE] User: what is list comprehension
Assistant: Concise way to create lists: [x * 2 for x in range(5)]. With condition: [x for x in numbers if x > 0].

[CODE] User: write a list comprehension example
Assistant: squares = [x ** 2 for x in range(10)]. Or filtered: evens = [x for x in numbers if x % 2 == 0].

[CODE] User: how to use range function
Assistant: range(5) gives 0-4. range(1, 6) gives 1-5. range(0, 10, 2) gives 0, 2, 4, 6, 8. Use in loops: for i in range(10).

[CODE] User: what is enumerate
Assistant: enumerate adds counter to iterable: for i, item in enumerate(items): print(f'{i}: {item}'). Start from 1: enumerate(items, 1).

[CODE] User: how to use enumerate in python
Assistant: for index, value in enumerate(my_list): print(index, value). Gives both position and element.

[CODE] User: what is zip function
Assistant: Combines iterables: list(zip([1, 2], ['a', 'b'])) gives [(1, 'a'), (2, 'b')]. Stops at shortest iterable.

[CODE] User: how to use zip in python
Assistant: for x, y in zip(list1, list2): print(x, y). Creates pairs from two lists.

[CODE] User: how to install python package
Assistant: Use pip: pip install package_name. For specific version: pip install package==1.2.3. Upgrade: pip install --upgrade package.

[CODE] User: what is pip
Assistant: pip is Python's package installer. Install packages: pip install numpy. List installed: pip list. Uninstall: pip uninstall package.

[CODE] User: how to create virtual environment
Assistant: python -m venv myenv. Activate: source myenv/bin/activate (Linux/Mac) or myenv\Scripts\activate (Windows).

[CODE] User: what is __init__ method
Assistant: Constructor for classes: def __init__(self, params): self.attribute = params. Called when creating instance: obj = MyClass(args).

[CODE] User: how to define a class
Assistant: class MyClass: def __init__(self, value): self.value = value. def method(self): return self.value. Create: obj = MyClass(10).

[CODE] User: what is self in python
Assistant: self refers to the instance in class methods. First parameter of instance methods. Allows access to attributes: self.attribute.

[CODE] User: how to comment code in python
Assistant: Single line: # This is a comment. Multi-line: '''This is a multi-line comment'''. Or: """Also multi-line""".
